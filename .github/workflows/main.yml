name: Deploy via SSH
on: [workflow_dispatch]
jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Deploy via SSH
      uses: fifsky/ssh-action@master
      with:
        command: |
          cd /tmp
          ls -a
        host: ${{ secrets.SSH_HOST_151 }}
        user: ${{ secrets.SSH_USERNAME_151 }}
        key: ${{ secrets.SSH_KEY_151 }}
        port: ${{ secrets.SSH_PORT_151 }}

    #- name: Deploy via SSH
      # uses: appleboy/ssh-action@master
      # run: python -c 'import os;print(os.environ)' && echo "Server:- ${{ secrets.SSH_HOST }}, UserName:- ${{ secrets.SSH_USERNAME }}, KEY:- ${{ secrets.SSH_KEY }}"
      #with:
      #  host: ${{ secrets.SSH_HOST }}
      #  username: ${{ secrets.SSH_USERNAME }}
      #  key: ${{ secrets.SSH_KEY }}
      #  script: |
      #    # git update to latest codebase on main branch
      #    cd /opt/socialswag-lambda-automation 
      #    script: whoami
