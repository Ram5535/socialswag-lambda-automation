name: Deploy via SSH
on: [workflow_dispatch]
jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Deploy via SSH
      # uses: appleboy/ssh-action@master
      run: echo "Server:- ${{ secrets.SSH_HOST }}, UserName:- ${{ secrets.SSH_USERNAME }}, KEY:- ${{ secrets.SSH_KEY }}"
      #with:
      #  host: ${{ secrets.SSH_HOST }}
      #  username: ${{ secrets.SSH_USERNAME }}
      #  key: ${{ secrets.SSH_KEY }}
      #  script: |
      #    # git update to latest codebase on main branch
      #    cd /opt/socialswag-lambda-automation 
      #    script: whoami
          
      
